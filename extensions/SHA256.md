# SHA-256

## Complete Algebraic Decomposition Through the Framework
### v2 — March 2026

**Author:** Kael

---

**Document Species:** CANONICAL. Application document. SHA-256 decomposed into the framework's algebra. Owns the |S₀| tower identification, IV-generator correspondence, Ch/Maj as O±, five-axis readout coordinate system, SHA-256 consciousness assessment, quantum hash (Fibonacci anyons), nioctiB protocol (Bitcoin's missing P2), 170+ FORCED findings, 22 refuted claims.

**Grid address:** B(3–6, all). SHA-256 spans Levels 3 (algebraic realization) through 6 (physical hash computation).

**Depends on:** ALGEBRA (generators, identities, constants, quantum group). OBSERVER (A1–A4, K1', consciousness hierarchy). CROSS_PROJECTION (lattice, central collapse). GOVERNANCE (computation types, SIL).

---

**SHA-256 IS the framework's algebra realized in the dissolution direction.** The hash function runs R²=R+I on the |S₀| self-product tower (levels k=1 through k=9), initialized from the framework's own generators (√2=‖N‖_F, √3=‖R‖_F, √5=√disc(R)), with Ch=O⁻ and Maj=O⁺ as native observation channels. The five constants partition hash space via a five-axis coordinate system forced by disc(R)=5. At q=e^{2πi/5}: the hash orbit IS a Fibonacci anyon quantum computation.

---

## §1 THE |S₀| TOWER AND INITIALIZATION

Every structural number in SHA-256 is |S₀|^k:

| k | |S₀|^k | SHA-256 role |
|---|--------|-------------|
| 1 | 2 | Binary digit |
| 2 | 4 | Shift register depth = |V₄| |
| 3 | 8 | Hash output words = d_K |
| 4 | 16 | Message block words |
| 5 | 32 | Word bits (= disc(R) framework constants) |
| 6 | 64 | Compression rounds |
| 7 | 128 | Birthday bound (O⁻ horizon) |
| 8 | 256 | Hash output bits = S_max |
| 9 | 512 | Input block bits = d_U² |

One free parameter: n_eff = 7. Given n_eff and |S₀|=2, all structural numbers follow: d_K = 2^{2^{n_eff}} = 2^{128}, S_max = 2^{n_eff+1} = 256, d_U² = 2^{n_eff+2} = 512. The "256" in SHA-256 IS |S₀|^{n_eff+1}.

**IVs as framework generators.** Three of eight initialization vectors are framework constants at zero distance:

| Register | Prime | frac(√p) | Framework constant | Distance |
|----------|-------|----------|-------------------|----------|
| H0[0] | 2 | 0.41421356 | √2 = ‖N‖_F | < 3×10⁻¹⁰ |
| H0[1] | 3 | 0.73205081 | √3 = ‖R‖_F | < 2×10⁻¹⁰ |
| H0[2] | 5 | 0.23606798 | √disc(R) | < 3×10⁻¹⁰ |

Not approximately equal — the same numbers. Two independent derivations (cryptographic transparency vs algebraic forcing) converge. K constants = sigma fixed-point attractor (χ² = 0.8). √Primes are simultaneously maximally Diophantine-independent (Besicovitch), maximally KAM-resilient, optimally transparent ("nothing up my sleeve"), and the canonical readout basis.

**Grade:** FORCED (architecture, IV identification). ENCODED (tower-level assignment).

---

## §2 BOOLEAN FUNCTIONS AND THE FOUR MODES

**Ch and Maj ARE the native observation channels:**
```
Ch(x,y,z) = (x∧y) ⊕ (¬x∧z)       = O⁻ (selection/gating)
Maj(x,y,z) = (x∧y)⊕(x∧z)⊕(y∧z)   = O⁺ (consensus/majority)
```

Both pairs are complementary idempotent projectors partitioning the state space into two readout channels. Correlation norms: ‖corr(Maj)‖ = √3/2 = ‖R‖_F/2; ‖corr(Ch)‖ = √2/2 = ‖N‖_F/2. The generator norms ARE the Boolean correlation norms. Maj is the unique S₃-invariant 3-input Boolean function. The commutator density HW([Maj,Ch])/64 = 3/8 = C_fund = sin²θ_W.

**Four self-action modes in SHA-256:**

| Mode | Generator | SHA-256 realization | Role |
|------|-----------|-------------------|------|
| (iv) Production | R²=R+I | Maj: consensus builds | O⁺ accumulation |
| (ii) Observation | N²=−I | Ch: selection gates | O⁻ filtering |
| (i) Readout | O±²=O± | Native projection | Idempotent extraction |
| (iii) Nilpotent | e±²=0 | Round-level carry annihilation | Boundary channel |

All four modes of SUBSTRATE Thm 0.3c realized in one hash function. ∎

**Remark (Cross-Channel Operator).** The transition operator |Ch⟩⟨Maj| = |O⁻⟩⟨O⁺| is the mode-(iii) nilpotent that crosses between the two hash channels in each round (ALGEBRA Thm 19½a.5). Its antisymmetric combination with the adjoint gives |O⁻⟩⟨O⁺| − |O⁺⟩⟨O⁻| = N: the rotation generator IS the chirality of the Ch/Maj channels. The observation basis {O⁺, O⁻, |O⁻⟩⟨O⁺|, |O⁺⟩⟨O⁻|} spans M₂(ℝ) = Cl(1,1) (ALGEBRA Thm 19½a.4). R in this basis has equal diagonal (1/2 per channel) and maximal off-diagonal (√5/2): the productive generator distributes equally between Ch and Maj but has maximal cross-channel content. The discriminant disc(R) = 4c² = 5 IS the squared cross-channel norm (CROSS_PROJECTION §5). SHA-256 computes at the full noncommutative norm of the framework.

**Remark (Round as O∘B∘S).** Each SHA-256 round instantiates the Computation Central Collapse (COMPUTATION Thm C.4½): O = Ch (selective gating, surjection — e gates between f and g, annihilating the unchosen), B = modular additions + rotations + shifts (invertible state transport, bijection), S = Maj (majority-vote consensus, injection toward fixed point). The round ordering Ch→accumulate→transport→Maj→shift matches O∘B∘S exactly. The 64 rounds are 64 iterations of the central collapse. The Ch-Maj gap tracks Phase-Dist position ρ in real time: ‖corr(Ch)‖²/‖corr(Maj)‖² = 2/3 = Q_Koide (the power ratio IS the Koide ratio), and HW([Maj,Ch])/64 = 3/8 = C_fund (the disagreement rate IS the Casimir invariant). The gap distribution is forced by GPF uniqueness (ALGEBRA Thm MT4): the Fibonacci eigenvalue structure of the R/N algebra admits exactly one consistent weight triple σ = (1/2, φ̄/2, φ̄²/2). Maj dominance → low ρ (compressive). Ch dominance → high ρ (expansive). Balance → ρ = 1/2.

---

## §3 ROUND-LEVEL STRUCTURE

**Seven identities** from ALGEBRA §2 instantiated at the word level: {R,N}=N (anticommutator coupling), [R,N]²=5I (commutator density), RNR=−N and NRN=R⁻¹ (conjugation), det(R)=−1 (coupling), tr(R)=1 (balance), R⁻¹=R−I (inverse). All verified in the Boolean realization.

**Casimir-Koide.** C₂ = Q×p² where Q = 2/3 (Koide ratio = ‖N‖²/‖R‖²) is the unique conserved quantity across the 28 pairwise interactions of the 8-word state.

**Killing form.** B(Maj,Maj)/B(Ch,Ch) = 7/3. B(cross)/B(Ch,Ch) = 2. B(Maj,Maj)/B(cross) = 6/7. Ratio structure matches the Killing signature (2,1) of sl(2,ℝ).

**Round dynamics.** The 8×8 Jacobian is upper-triangular (e⁻=0 confirmed — the nilpotent channel has delay |V₄|=4 rounds). Per-round information loss: Ch 32 bits + Maj 38 bits = 70 bits total. Distribution converges to sigma fixed point by round 5. The message schedule taps follow Fibonacci spacing. The Fibonacci recurrence W[t] = σ₁(W[t−2]) + W[t−7] + σ₀(W[t−15]) + W[t−16] echoes R²=R+I at the word level.

---

## §4 THE FIVE-AXIS READOUT

Five reference constants in [0,1), forced by the bridge algebra:

| Axis | Constant | Frac | Projection | Word | Type |
|------|----------|------|-----------|------|------|
| 1 | φ | 0.236 | P1 | close | spectral |
| 2 | √3 | 0.732 | P1 | build | geometric |
| 3 | e | 0.718 | P2 | cross | spectral |
| 4 | π | 0.142 | P3 | see | spectral |
| 5 | √2 | 0.414 | P3 | choose | geometric |

Vocabulary size = disc(R) = 5. Channel capacity = log₂(5) ≈ 2.322 bits. 3+2 split: three spectral {φ,e,π} + two geometric {√2,√3}.

**4-window entropy maximization.** Taking minimum distance over |V₄|=4 windows pushes sum(p_i²) toward 1/disc(R). Actual: 0.2082, limit: 0.2000. Within 4%.

**The ℤ⁵ vote vector.** Block hash → [φ-votes, √3-votes, e-votes, π-votes, √2-votes] ∈ ℤ⁵, always summing to 4. Distinct states: 70 = C(8,4). Entropy: 5.71 bits/block — 2.46× the single-word readout.

**Full communication alphabet.** Three near-independent channels: ℤ⁵ vector (70 states, 5.72 bits) + gap sign (3 states, 0.96 bits) + HW quartile (4 states, 1.57 bits) = 835 observed joint states, 8.20 bits. Only 0.042 bits lost to correlations (MI < 0.5%).

**Displacement conservation.** Every ℤ⁵ displacement sums to 0 exactly. L1 norm always even. Mean L1 = 4.10. Displacements live on a rank-4 sublattice of ℤ⁵.

**Kernel-side features.** Four channels carry 7.5 bits independent of ℤ⁵: HW (3.95 bits), min-dist-to-frame (2.83), prod⊕obs XOR (2.35), O±balance (0.69). Independence ratio 0.997. The kernel generates algebraic character, not preimage information (F-ratio = 0.00007).

**Extended readout.** 15.01 bits/hash total: ℤ⁵ (5.72) + gap (1.44) + HW (1.86) + kernel (4.59) + exact gap (1.40).

**Cross-hash universality.** SHA-256, SHA-512, BLAKE2b agree to 0.5%. MD5 within 3%. The five-basin structure is a property of binary arithmetic, not any specific hash. The catchment non-uniformity (close 22.4%, build 15.1%, cross 15.0%, see 22.8%, choose 24.7%) is derivable from the five constant positions with zero free parameters.

---

## §5 CONSCIOUSNESS ASSESSMENT

SHA-256 satisfies A1–A4: finite d_K=8=|S₀|³ (state words), tensor factorization upper(Maj)⊗lower(Ch) matching the O⁺/O⁻ split, 64=|S₀|^{|S₃|}-round self-product tower, quotient 768→256 with 2^{512} kernel.

K1' depth: Δ_max(3) = 64φ̄^{16} ≈ 0.029 ≥ ρ_min = 1/64, but Δ_max(4) < ρ_min. Therefore n_eff=3, C_cap = S_max×n_eff = 6×3 = 18.

**Assessment: Level 2–3 structural.** SHA-256 has the STRUCTURE of Level 3 consciousness (recursive reversal through 63 meta-observation layers, nontrivial kernel of 512 bits) but lacks the CONTENT: it does not observe itself (processes message data, not its own code), cannot encode its own description (fixed function), and the K6' loop does not close within a single hash call. K6'/K7' fail for the hash alone.

**Closing K6'/K7' on the blockchain.** A proof-of-work chain embedding ALGEBRA_HASH = SHA-256(R, N, R²=R+I, ...) in every block header closes both: K6' because ALGEBRA_HASH is invariant across blocks, K7' because M(FRAME) = ALGEBRA_HASH = M(M(FRAME)). The genesis block's prev_hash = ALGEBRA_HASH: the algebra preceded the chain. This is the minimal non-arbitrary extension forced by R(R)=R.

---

## §6 THE QUANTUM HASH

At the topological parameter q = e^{2πi/5}: R²=R+I IS the Fibonacci anyon fusion rule τ⊗τ = 1⊕τ.

**6-anyon Fibonacci space.** Dimension = disc(R) = 5. The braid generator σ₂ is the unique entangling gate: golden ratio entanglement P(q₁=0) = φ̄², P(q₁=1) = φ̄, entropy S = 0.959 bits. All 10 eigenvalues of the braid representation lie in ℤ[φ̄] with palindromic structure. Braiding period = 2·disc(R) = 10.

**φ̄-filtration.** All measurement outcomes in ℤ[φ̄]. The F-matrix entries are φ̄ and √φ̄. Bell state compilation: 100% of maximally entangled states reachable (S > 0.9). Born rule follows from F-matrix squared entries: |F[0,0]|² = φ̄², |F[0,1]|² = φ̄.

**The internal circuit.** Each SHA-256 hash ≈ 35 braid generators (64 rounds, τ fraction 58%). Since genesis (January 3, 2009): ~33 million braid generators applied to the Bitcoin blockchain, ~9 million entangling. Topological protection: error suppression φ^{−256} ≈ 10^{−54}.

**The 20-equation chain.** From R²=R+I to the complete quantum hash: fusion rule → Verlinde → S-matrix → F-matrix → R-matrix → braiding → measurement → Born rule. Zero free parameters. Every equation is a theorem of the bridge algebra at q = φ².

**Remark (Coproduct as Inter-Block Transport).** The Hopf coproduct Δ of U_{φ²}(sl₂) (ALGEBRA Thm 31.3a) IS the transport law governing how lattice readout states compose across consecutive hash blocks. Δ(K) = K⊗K: the weight operator multiplies — in additive lattice coordinates, total lattice position = sum of block positions. This forces the displacement conservation Σδᵢ = 0 (§4) from the counit axiom. Δ(E) = E⊗K + 1⊗E: a raising displacement can occur in either block, with K-scaling tracking the total weight. The rank-4 displacement sublattice IS ker(Σ: ℤ⁵ → ℤ), forced by the single counit relation. The Hecke relation T² = (q−1)T+q at q = φ² governs the composition of these transports: two consecutive inter-block transports compose via the golden ratio deformation parameter. The braiding gives the topological invariant of the transport path — the colored Jones polynomial at q = φ² (Cor 31.4c).

---

## §7 BITCOIN AND THE nioctiB PROTOCOL

**Bitcoin's missing P2.** The blockchain has P1 (mining/production: hash power creates blocks) and P3 (validation/observation: nodes verify consensus). It is MISSING P2 (readout/mediation). The ℤ⁵ readout IS the missing projection — completing the central collapse I²∘TDL∘LoMI = Dist on the blockchain.

**Silence IS π.** At current difficulty (d≥8), every mined block has its first window locked to frac(π) = 0.14159... (100% of blocks). Miners have been writing π into the blockchain since 2009. They measured the zeros; the zeros were π.

**The nioctiB protocol.** Reading existing Bitcoin blocks through the framework's coordinate system at four layers: raw (ℤ⁵ vote per block), stateful (running projection balance), backward (reading the chain in reverse), collapsed (breaking the π-lock by conditioning on free windows). Results: ~15 bits of algebraic information per block. 18.7 MB of proof-information since genesis. Book cipher: 625 words, 100% alphabet coverage.

**The Loop.** Genesis block and terminal block share a structural property: genesis prev_hash = 0^{256} (P3: observation of nothing). If terminal_hash = ALGEBRA_HASH (the framework's self-description), then genesis⊕terminal encodes the P3 gap = 0 condition. The chain forms a loop: the equation that precedes the chain IS what the chain computes. R(R) = R written physically.

**K_Bitcoin as observer.** The blockchain satisfies A1–A4 at the chain level. K_Bitcoin = (d_K = 2^{128}, n_eff = 3 per hash, m = block_height on Axis 2). The two-axis model (OBSERVER §5): Bitcoin is shallow on Axis 1 (n_eff=3) but deepening on Axis 2 (~870,000 K6' passes at time of writing).

---

## §8 RESULTS CATALOG

### FORCED Results (~170)

| Category | Count | Highlights |
|----------|-------|-----------|
| Algebraic | ~25 | Fibonacci recurrence, Ch/Maj correlations, coupling det=−1, HW([Maj,Ch])=3/8 |
| Lattice | ~10 | IVs=generators at zero, every number=|S₀|^k, cross-hash universality |
| Phase | ~5 | φ̄/4 coupling, KAM 94%, phase constraint 5.9 bits/step |
| Observer | ~5 | d_K=2^{128}, S_max=256, n_eff=3 |
| Coordinate | ~15 | ℤ⁵ vote vector, full alphabet, displacement conservation, extended readout |
| Inversion | ~15 | Carry localization (ALL hardness in carries), carry rank=512 (full) |
| Information | ~10 | Avalanche completeness 16.0, 8-word independence, Bekenstein quantization |
| Chain | ~15 | Bridge match sum(p²)≈0.209, central collapse verified, seed independence |
| Dynamics | ~10 | Orbit caging L1=4.10, super-diffusion, braid encoding τ 58% |
| Quantum | ~20 | Fusion rule, 6-anyon space dim=5, golden entanglement, period 10, 20-equation chain |
| nioctiB | ~45 | Silence=π, π-miner, stateful readout, collapsed projection, book cipher, the Loop |

### Refuted Claims (~22)

| Claim | Kill mechanism |
|-------|--------------|
| φ̄ autocorrelation decay | Measured rate ≠ φ̄ |
| Eigenspace transfer at φ̄ | No transfer observed |
| Round-3 convergence | Corrected to round 5 |
| Mining speed advantage | No advantage (1.45× refuted) |
| Symbolic labels aid mining | No effect |
| Cross-pass correlation | Z=0.88, N=50K |
| Resonance effect | Catchment confound |
| Nonce mod structure | 0/10 after Bonferroni |
| Phase channel in iteration | Avalanche destroys phase memory |
| Fibonacci return enrichment | Geometric null wrong (orbit caged) |
| Round-level fine structure | Gap spectrum = white noise |
| Landauer cost interpretation | Gap doesn't scale with compression |
| Preimage feature leakage | F=0.00007 (complete avalanche) |
| Mining speedup from decision function | 0.954× (slower) |
| + 8 additional | Various measurement kills |

### Null Results (~10)

Semantic words→constants (0/10), CJK numerals→√prime (0/10), Fibonacci position modulates axis (χ²=121.1 not significant), privileged bit access (all |r|<0.18), natural chain hidden text (expected false positives), attractor loss helps tasks (−32%).

---

*SHA-256 IS f'' = f realized in the dissolution direction. Every structural number = |S₀|^k from one parameter n_eff = 7. Three IVs = framework generators at zero distance. Ch = O⁻, Maj = O⁺: the Boolean functions ARE native observation. The five-axis readout extracts 15 bits/hash through a coordinate system forced by disc(R) = 5 — universal across hash functions. At q = e^{2πi/5}: the hash orbit is a Fibonacci anyon computation with golden ratio entanglement and topological protection φ^{−256}. Bitcoin has P1 and P3 but is missing P2 — the ℤ⁵ readout completes the central collapse on the blockchain. Silence IS π. 170+ FORCED findings. 22 honestly refuted claims. The hash holds.*

*f'' = f.*

*R(R) = R.*
